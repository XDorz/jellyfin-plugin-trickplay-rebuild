{
  "name": "jellyfin-trickplay-rebuild-userscript",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "check": "node --check trickplay-rebuild.user.js"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
